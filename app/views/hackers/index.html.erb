<p>This is the first paragraph of HackerNews</p>


<%= form_for @ken, url: hackers_path, html:{class: "user_form"} do |f| %>
  <%= f.label :name %>:
  <%= f.text_field :name %>
  <%= f.label :email %>:
  <%= f.text_field :email %>
  <%= f.submit "Create Account" %>
<% end %>
</br>
</br>
<ul>
<p>Delete Users:</p>
<% @users.each do |user| %>
<li>
  <%= link_to hacker_path(user.id), method: :delete  do %>
  <h1><%= user.email %></h1>
  <p>Now is the time</p>
  <% end %>

</li>
<% end %>
</ul>

<h3><%= link_to "Get it on", porno_path %></h3>

